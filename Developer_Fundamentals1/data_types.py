# Fundamental Data Types in Python

int number = 42
float pi = 3.14
str greeting = "Hello, World!"
bool is_active = true
list numbers = [1, 2, 3, 4, 5]
dict person = {"name": "Alice", "age": 30}
tuple coordinates = (10.0, 20.0)
None none_type = type(None)
set unique_numbers = {1, 2, 3, 4, 5}
complex_number = 2 + 3j # Complex number    

#Classes - Custom Data Types

#Specialized Data Types -> not  built in python but can be imported from packages/ modules;

from datetime import datetime, date, time, timedelta
from decimal import Decimal
from fractions import Fraction
from uuid import UUID, uuid4